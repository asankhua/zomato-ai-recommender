# Copy to secrets.toml and fill in. For Streamlit Cloud, add these in the dashboard.
# Do not commit secrets.toml.

# For AI-powered recommendations (Streamlit Cloud standalone)
GROQ_API_KEY = "your_groq_api_key_here"

# Optional: if using a separate backend API instead of embedded logic
# API_BASE_URL = "https://your-backend-url.onrender.com"
