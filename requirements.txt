# Streamlit app: works standalone (Streamlit Cloud) or with API (local)
# Standalone mode embeds Phase 3+4 logic; no separate backend needed.

streamlit>=1.28.0
requests>=2.28.0
python-dotenv>=1.0.0
groq>=0.4.0
